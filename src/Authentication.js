// var UserModel = require('./User');
//
// authentication = function(submitted_email, submitted_password) {
//   UserModel.find({email: submitted_email}, function(err, users) {
//     if (submitted_password !== users[0].password) { res.send('Incorrect password') };
//     if (submitted_password == users[0].password) { res.redirect('/home') };
//   });
// };
